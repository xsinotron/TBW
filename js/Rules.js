/**
 * Définition des règles du jeu
 */
(function (window) {
    'use strict';
    var $       = window.$,
        console = window.console,
        alert   = window.alert,
        Rules  = function () {};
    window.RUL = RUL;
})();
